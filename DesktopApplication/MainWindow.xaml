<Window x:Class="DesktopApplication.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DesktopApplication"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">


    <DataGrid x:Name="RoomGrid" AutoGenerateColumns="False" AlternatingRowBackground="LightBlue" AlternationCount="2" >
        <DataGrid.Columns>
            <DataGridTextColumn Header="Room Nr" Binding="{Binding RoomID}"/>
            <DataGridTextColumn Header="Type" Binding="{Binding Type}"/>
            <DataGridCheckBoxColumn Header="Vasket" Binding="{Binding Vasket}"/>
            <DataGridCheckBoxColumn Header="Opptatt" Binding="{Binding Opptatt}"/>
            

        </DataGrid.Columns>
    </DataGrid>





</Window>
